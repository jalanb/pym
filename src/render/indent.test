The indent module
=================

    >>> import indent
    >>> print indent.__doc__
    Indentation for renderers

    >>> indenter = indent.Indenter()
    >>> indenter.render('text') == 'text'
    True
    >>> indenter.indent()
    >>> indenter.render('text') == '    text'
    True
    >>> indenter.indent()
    >>> indenter.render('text') == '        text'
    True
    >>> indenter.indent()
    >>> indenter.render('text') == '            text'
    True
    >>> indenter.dedent()
    >>> indenter.render('text') == '        text'
    True
    >>> indenter.indent()
    >>> indenter.render('text') == '            text'
    True
    >>> indenter.dedent()
    >>> indenter.dedent()
    >>> indenter.render('text') == '    text'
    True
    >>> indenter.dedent()
    >>> indenter.render('text') == 'text'
    True
    >>> indenter.dedent()
    Traceback (most recent call last):
    ...
    ValueError: Cannot dedent

    >>> indenter = indent.Indenter('\t')
    >>> indenter.indent()
    >>> indenter.indent()
    >>> indenter.render('text') == '\t\ttext'
    True

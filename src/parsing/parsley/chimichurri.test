The chimichurri module
======================

    >>> import chimichurri
    >>> print chimichurri.__doc__
    Chimichurri (IRL is a sauce which) uses parsley
    ...

    >>> grammar = chimichurri.minimal_grammar()
    >>> grammar('x').target() == 'x'
    True

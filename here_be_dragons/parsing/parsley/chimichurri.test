The chimichurri module
======================

    >>> import chimichurri
    >>> assert "is a sauce" in chimichurri.__doc__
    ...

    >>> grammar = chimichurri.minimal_grammar()
    >>> assert grammar('x').target() == 'x'

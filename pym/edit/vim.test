The vim module
==============

    >>> import vim
    >>> print vim.__doc__
    Provide keys similar to vim's

    >>> vim.keys['j'] == 'down'
    True
    >>> vim.keys['k'] == 'up'
    True
    >>> vim.keys['h'] == 'up'
    False

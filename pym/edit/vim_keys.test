The vim module
==============

    >>> from pym.edit import vim_keys as vim
    >>> print vim.__doc__
    Provide keys similar to vim's

    >>> vim.keys['j'] == 'down'
    True
    >>> vim.keys['k'] == 'up'
    True
    >>> vim.keys['h'] == 'up'
    False

    >>> class Fred(object):
    ...     def up(self):
    ...         print 'Hello World'

    >>> vim.call(Fred(),'k')
    Hello World
